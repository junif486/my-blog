# my-blog

마크다운(.md) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트.

## 핵심 원칙

- **프레임워크 없음.** React/Vue/Next 등 사용 금지. 순수 HTML, CSS, JavaScript(vanilla JS)로만 구현한다.
- **빌드 도구 없음.** webpack/vite 등 번들러 없이, 브라우저에서 바로 동작해야 한다. 필요하면 `<script type="module">` 정도만 사용.
- **정적 사이트.** 서버 사이드 렌더링이나 백엔드 없이, 브라우저(클라이언트) 또는 간단한 빌드 스크립트에서 마크다운을 HTML로 변환한다.

## 기능 요구사항

- `posts/` (또는 유사한) 디렉터리의 `.md` 파일을 읽어 글 목록과 상세 페이지로 렌더링한다.
- 마크다운 파싱은 외부 대형 프레임워크 없이 가벼운 파서(직접 구현 또는 단일 파일 라이브러리)로 처리한다.
- 각 글은 제목, 날짜, 본문을 최소한으로 포함한다. Frontmatter(YAML 메타데이터) 지원 여부는 구현 시 결정한다.

## 디자인 가이드라인

- **깔끔하고 읽기 좋은 타이포그래피**: 넉넉한 줄간격(line-height), 적절한 본문 폭(예: 65~75자 기준 `max-width`), 가독성 좋은 폰트 스택.
- **다크 모드 지원**: `prefers-color-scheme` 미디어쿼리를 기본으로 하고, CSS 커스텀 프로퍼티(`--bg`, `--text`, `--accent` 등)로 색상을 관리한다. 가능하면 수동 토글도 고려.
- **모바일 반응형**: 미디어쿼리 또는 유동적 레이아웃(flexbox/grid + relative units)으로 작은 화면에서도 잘 보이도록 한다. 가로 스크롤이 생기지 않도록 주의.
- 불필요한 장식보다 콘텐츠 가독성을 우선한다.

## 코드 스타일

- 불필요한 추상화나 설정(빌드 파이프라인, CSS 프레임워크, 패키지 매니저 의존성) 추가 금지 — 요구사항에 없는 기능은 만들지 않는다.
- 주석은 WHY가 명확하지 않은 경우에만 최소한으로 작성한다.
